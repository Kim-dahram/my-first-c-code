/*
#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main(void)
{
	char temp[80];
	char *str[2];
	int num;
	
	
		printf("이름:");
		gets(temp);
		str[0]=(char*)malloc(strlen(temp)+1);
		strcpy(str[0],temp); 
		
		{
	
		printf("나이:");
		scanf("%d",&num);
		getchar();
}

			printf("전화번호:");
		gets(temp);
		str[1]=(char*)malloc(strlen(temp)+1);
		strcpy(str[1],temp); 


		printf("%s\n%d\n%s\n",str[0],num,str[1]);
	

		free(str[0]);
		free(str[1]);

	
			

	
	return 0;
}
*/
