/*


#include <stdio.h>

int main(void)
{
	int a = 30; 
	int res;
	
	res = (a>10) && (a<20);        
	printf("(a > 10) && (a<20) : %d\n", res); //양쪽 모두 참일때 참 
	res = (a<10) || (a>20);
	printf("(a < 10) || (a>20) : %d\n", res); // 하나라도 참일때 참 
	res = !(a>=30);
	printf("!(a >=30) : %d\n", res);  //거짓이면 참, 참이면 거짓 
	
	return 0;
}
*/
