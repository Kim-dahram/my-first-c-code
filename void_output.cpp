/*
#include <stdio.h>

int func() 
{
	//정수 100을 출력. 출력이 있으면 무조건 리턴 있어야함 
return 100;
}

int main ()
{
	int ret;
	ret = func();
	printf("전달받은 값 : %d", ret); 
	//	printf("전달받은 값 : %d", func());  // 이렇게 사용하면 함수 값이 매번 휘발되기때문에 위처럼 값 저장 
	return 0;
}
*/
