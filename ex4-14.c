/*
#include <stdio.h>

int main(void)
{
	int a = 10, b=5;
	int res;
	
	res=a/b*2;
	printf("res=%d\n",res);  // 우선순위 같을시 왼쪽부터 
	res= ++a*3;
	printf("res=%d\n",res);  // a 값을 1증가하고 3곱 
	res=a>b&&a!=5;
	printf("res=%d\n",res); // a>b결과와 a!5의 결과를 &&연산// 
	res=a%3==0;
	printf("res=%d\n",res); // a%3의 값이 0과 같은지 확인 
	
	return 0;
}

*/

