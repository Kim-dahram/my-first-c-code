/*
#include <stdio.h>

int main(void)
{
	int a, b;
	int sum, sub, mul, inv;
	
	printf("숫자 두개를 입력하세요. : ");
	scanf("%d %d", &a, &b); //주소연산자 & 빼먹으면 안된다 ***************** 
	sum = a + b;
	sub = a - b;
	mul = a * b;
	inv = a / b;
		
	printf("덧셈 : %d\n", sum);
	printf("뺄셈 : %d\n", sub);
	printf("곱셈 : %d\n", mul);
	printf("a의 음수 연산 : %d\n", inv);
	
	return 0;
}
*/
