/*
#include<stdio.h>

	
   double cal(void);
	
	
	int main(void)
	{
		double result;
		
		result = cal();
	
	    printf("%lf\n", result);
		return 0;
	}

double cal()
{
		int a, b, sum, sub, mul, c, d; 
		double inv;
		char ch;
		printf("ют╥б");
		scanf("%d%c%d", &a,&ch,&b);
		if (ch=='+')
		{
		sum=a+b;
		return sum;
		}
		if (ch=='-')
		{
		sub=a-b;
		return sub;
		}
		if (ch=='*')
		{
		mul=a*b;
		return mul;	
		}
		if (ch=='/')
		{
		(inv = ((double)a) / ((double)b) ); 

		
		return inv;	
		}		
		
		
}

*/
