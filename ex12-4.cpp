/*
#include <stdio.h>
int main(void)
{
	char str[7]; //캐릭터형태의 배열 (여러글자 써야하니까 ) 
	printf("공백이 포함된 문자열입력 : ");
	gets(str);  //공백까지 가져오는 함수이기 때문에 보통 스캔안쓰고 겟츠 씀... 
	printf("문자열은 %s입니다.",str);
	return 0;
}
*/
